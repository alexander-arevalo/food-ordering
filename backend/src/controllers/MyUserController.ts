import { Request, Response } from "express";

const createCurrentUser = (req: Request, res: Response) => {
  //user exist ? create user : call user account
};

export default {
  createCurrentUser,
};
